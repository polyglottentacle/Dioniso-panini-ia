# Firebase Configuration (Optional - for testing without subscriptions)
VITE_FIREBASE_API_KEY=test_api_key
VITE_FIREBASE_PROJECT_ID=test_project_id
VITE_FIREBASE_APP_ID=test_app_id

# Database Configuration (Mock for testing frontend)
# Using a placeholder URL - the app will start but database operations won't work
DATABASE_URL=postgresql://test:test@localhost:5432/dioniso_test

# Node Environment
NODE_ENV=development
